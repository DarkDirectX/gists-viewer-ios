language:
  - swift

osx_image: xcode10.2

branches:
  only:
    - master
    - develop

script:
  - xcodebuild -scheme GistsViewerTests -enableCodeCoverage YES -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 6,OS=12.2' test | xcpretty

after_success:
  - bash <(curl -s https://codecov.io/bash) -t {$CODECOV_TOKEN}
